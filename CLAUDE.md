# BR4M.tv

Same rules as [AGENTS.md](./AGENTS.md). Read that file first.

**Language lock:** the live site is English only. Never write Dutch or any other language into HTML, CSS content, JavaScript UI strings, or metadata. YouTube video titles from the feed are the only exception (source titles, not UI).
